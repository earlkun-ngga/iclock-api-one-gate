const config = {
    db: {
        host: "localhost",
        user: "root",
        password: "",
        database: "iclock",
    },
    server: {
        version: "1.0.1",
        name: "TAS_API",
        protocol_version: "3.1.1",
        timeout: 10
    }
};

module.exports = config;